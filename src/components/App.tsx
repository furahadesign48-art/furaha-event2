@@ .. @@
 import ProtectedRoute from './components/ProtectedRoute';

 interface Tem}
plateData {
@@ .. @@
 function AppContent() {
   const { user, isAuthenticated, logout } = useAuth();
   const [showDashboard, setShowDashboard] = useState(false);
   const [showWeddingTemplate, setShowWeddingTempl
}ate] = useState(false);